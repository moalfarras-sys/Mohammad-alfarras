# Public Web Audit and Optimization - 2026-06-13

## Scope

Audited and updated the public Next.js app in `apps/web`, covering English and Arabic routes, shared layout, SEO, public forms, CV exports, MoPlayer routes, internal links, static generation, bundle loading, responsive behavior, and mobile Lighthouse performance.

## Route and Shell Audit

The current repository already had one shared localized shell for the active public site. The production symptoms described in the original brief were caused by older deployed output rather than three active layout implementations in the checked-out source.

Active localized route families:

- `/{locale}`
- `/{locale}/about`
- `/{locale}/services`
- `/{locale}/work`
- `/{locale}/work/[slug]`
- `/{locale}/apps`
- `/{locale}/apps/moplayer`
- `/{locale}/apps/moplayer/classic`
- `/{locale}/apps/moplayer2`
- `/{locale}/apps/moplayer-pc`
- `/{locale}/youtube`
- `/{locale}/cv`
- `/{locale}/contact`
- `/{locale}/activate`
- `/{locale}/support`
- `/{locale}/privacy`
- `/{locale}/moplayer/setup`

All active English and Arabic pages use the same navbar, footer, mobile dock, skip link, locale behavior, design tokens, and RTL-aware shell.

Permanent single-hop redirects:

- `/ar/projects` -> `/ar/work`
- `/en/projects` -> `/en/work`
- `/ar/blog` -> `/ar/work`
- `/en/blog` -> `/en/work`

The weather and football UI were removed from the public portfolio. Their API routes were retained because the Android and Windows products still consume those services; deleting those APIs would have broken non-web clients.

## Cleanup

Deleted unused presentation code:

- `components/layout/atmospheric-background.tsx`
- `components/layout/digital-os-client-effects.tsx`
- `components/site/cinematic-overlay.tsx`
- `components/ui/Reveal.tsx`
- `components/ui/SwipeRail.tsx`
- `components/theme-provider.tsx`
- `components/layout/use-theme-mode.ts`
- `lib/live-data.ts`
- `lib/matches-live.ts`
- `lib/media.ts`
- `lib/motion.ts`
- `lib/validation.ts`
- `lib/weather-live.ts`
- `types/arabic-persian-reshaper.d.ts`
- `public/sw.js`
- `public/images/moplayer-ui-mock-final.png`
- `public/images/moplayer_hub_hero_bg.png`

Removed unused web dependencies, including the web-local PDF/Chromium/Puppeteer/YouTube parser packages, `next-themes`, `arabic-persian-reshaper`, `@testing-library/react`, `eslint-config-prettier`, and `shadcn`. Added the directly imported `server-only` package explicitly. Admin-owned equivalents were not removed.

Removed dead CV percentage UI, coffee joke counters, public weather/football cards, obsolete theme switching, duplicated client dispatch code, and route-wide Framer Motion loading.

## Content and Product Consistency

- Standardized the public name to `Mohammad Alfarras`.
- Added `content/site-data.ts` for identity, socials, stable YouTube fallback metrics, language levels, and shared formatting.
- Corrected the broken `1.5K+ K` views presentation to the real million-scale metric.
- Removed skill percentages and replaced them with shipped-project and product proof.
- Removed visitor-facing admin implementation copy from the contact flow.
- Added a complete player-only legal disclaimer to the MoPlayer hub and Classic product page.
- YouTube data is fetched server-side with 24-hour revalidation when API credentials are available.
- Standardized both locale fallbacks and generated CV exports on the most detailed existing timeline: Rhenus Home Delivery from November 2023 to present and Stocubo from October 2019 to November 2022. The contradictory IKEA and generic `2018-2021` fallback entries were removed.

## Interaction Verification

- Contact hero CTA now anchors to `#inquiry-form`.
- Contact validation and localized errors work.
- Honeypot submissions now return an ignored success instead of failing schema validation.
- Language switching preserves the equivalent route and query string.
- CV PDF and DOCX endpoints return valid files.
- MoPlayer config, release, activation, APK links, support, privacy, and legal routes were checked.
- Mo AI now loads only after the user clicks its launcher or a project-specific “Ask Mo Ai” action. The contextual prompt is preserved while the lazy widget loads, and the full chat bundle is absent from initial page load.
- Latest APK links resolve directly to their release asset in one redirect instead of passing through a second release endpoint.

Automated local integration result:

- 50 sitemap URLs checked
- 59 unique internal paths checked
- 0 broken links
- 0 redirect chains
- PDF signature valid, 665063 bytes
- DOCX ZIP signature valid, 10768 bytes
- Honeypot ignored successfully

## SEO

- Fixed duplicated title templates.
- Added localized unique SEO data without obsolete meta keywords.
- Added self-referencing canonicals and `en`, `ar`, and `x-default` alternates.
- Generated a localized sitemap with real `lastModified` values.
- Updated robots rules for public pages, API routes, admin routes, and drafts.
- Added or corrected Person, WebSite, SoftwareApplication, VideoObject, BreadcrumbList, FAQPage, CollectionPage, ContactPage, ProfilePage, and CreativeWork structured data.
- Enforced one H1 on checked public routes and corrected MoPlayer heading order.
- Root `/` redirects permanently to `/en`.

Search Console and Bing sitemap submission remain post-deploy operational tasks requiring account access.

## Performance

Major changes:

- Converted public routes to SSG/ISR where request-time data was unnecessary.
- Removed root `headers()` usage that made the entire public site dynamic.
- Split page-specific client components so home no longer includes Work, CV, Apps, Contact, or project-detail code.
- Split the 10,735-line global stylesheet into route-owned CV, Contact, Work, YouTube, Apps, Activation, MoPlayer Pro, and MoPlayer Classic styles. The shared global file is now 4,760 lines and its home-route transfer fell from about 48.4 KB to 29.1 KB.
- Converted CV and the MoPlayer hub to Server Components.
- Converted the global 404 page to a Server Component.
- Replaced automatic assistant loading with click-to-load.
- Reduced mobile backdrop blur and retained reduced-motion handling.
- Added accurate responsive image sizes and quality controls.
- Added 384px and 480px image breakpoints.
- Reduced the English and Arabic font critical path and limited the site to two families.
- Added `content-visibility` to long below-the-fold CV and MoPlayer sections.
- Replaced the oversized browser icon request with the 64px favicon, added a dedicated 180px Apple icon, corrected the PWA manifest icon dimensions, and switched social metadata to the generated 1200x630 Open Graph image.
- Recompressed the 512px transparent site icon from 67.7 KB to 22.1 KB without changing its dimensions or appearance.
- Added generated Open Graph and Twitter metadata routes to the proxy allowlist so their signed Next.js URLs are served directly instead of being locale-prefixed into 404 pages.
- Disabled automatic route prefetch on the persistent navbar, footer, mobile dock, locale switcher, homepage links, and MoPlayer product links. This removed the Contact and MoPlayer Pro route bundles from unrelated first loads.

Modern-browser initial JavaScript for all five acceptance routes is approximately `170.2 KB` transferred, down from approximately `409 KB` during the audit. The MoPlayer hub was reduced from approximately `236.8 KB` to `170.2 KB` by removing unnecessary product-page prefetching.

Mobile Lighthouse comparison:

| Route | Before Perf | After Perf | After A11y | After BP | After SEO | After LCP | After TBT | CLS |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| `/en` | 68 | 91 | 100 | 100 | 100 | 3.37s | 80ms | 0 |
| `/ar` | 57 | 91 | 100 | 100 | 100 | 3.42s | 39ms | 0.007 |
| `/en/cv` | 71 | 94 | 100 | 100 | 100 | 2.99s | 87ms | 0 |
| `/ar/cv` | 60 | 93 | 100 | 100 | 100 | 3.15s | 61ms | 0.008 |
| `/en/apps/moplayer` | 61 | 91 | 100 | 100 | 100 | 3.38s | 48ms | 0.001 |

Performance, accessibility, best-practices, SEO, approximate JavaScript, CLS, and TBT budgets are met in isolated runs; Lighthouse has normal run-to-run variance of roughly 1-3 performance points. The strict `LCP < 2.5s` target is not yet met under the local Lighthouse mobile profile. A measured Next.js `inlineCss` experiment increased response weight and reduced performance from 94 to 91, so it was removed instead of shipping an experimental regression.

## Verification

Passed:

```powershell
npm run verify:web
npm run verify:admin
npm run verify:production
npm --prefix apps/web run test:e2e
```

The web verification includes:

- TypeScript
- ESLint
- Next.js production build
- 95 generated pages
- Vitest: 3 files, 12 tests
- Playwright: 26 tests across desktop and mobile Chromium

Both public web and admin apps now use the current stable compatible release, Next.js `16.2.9` with `eslint-config-next` `16.2.9`.

`npm audit --omit=dev` still reports three transitive advisories:

- `esbuild` `0.27.7`, loaded by Vite/Vitest/tsx development tooling. The fixed `0.28.1` release is outside the current `tsx` compatibility range, so no unsafe lockfile override was added.
- A nested PostCSS `8.4.31` copy bundled under Next.js.
- Next.js is consequently listed as affected through that nested PostCSS copy. npm proposes downgrading to Next.js `9.3.3`, which is not a valid or safe remediation for this application.

These advisories should be rechecked when compatible upstream releases are available. They do not justify downgrading the framework or forcing an incompatible build-tool override.

Visual browser checks covered mobile English and Arabic home, CV, MoPlayer, and contact surfaces with correct direction, one H1, no horizontal overflow, and no console errors.

## Factual Owner Check

The shipped site is internally consistent, but the owner should still confirm that the selected source timeline is factually correct:

- Rhenus Home Delivery: November 2023 to present
- Stocubo: October 2019 to November 2022
- No IKEA entry
