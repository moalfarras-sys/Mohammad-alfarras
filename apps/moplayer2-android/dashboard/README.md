# Moved

The MoPlayer Vite dashboard now lives in the main monorepo:

`Moalfarrasappseit/apps/moplayer-dashboard`

From the monorepo root:

```bash
npm install
npm run dev:moplayer-dashboard
```

Do not add a separate `package-lock.json` here; the workspace lockfile is at `Moalfarrasappseit/package-lock.json`.
