<?xml version="1.0" encoding="utf-8"?>
<!-- MoPlayer — Play Button (vivid cyan fill for maximum visibility) -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- FOCUSED state -->
    <item android:state_focused="true">
        <layer-list>
            <item android:left="-5dp" android:top="-5dp" android:right="-5dp" android:bottom="-5dp">
                <shape android:shape="rectangle">
                    <corners android:radius="18dp" />
                    <gradient
                        android:type="radial"
                        android:gradientRadius="100dp"
                        android:centerX="0.5"
                        android:centerY="0.5"
                        android:startColor="#7000F5FF"
                        android:endColor="#00000000" />
                </shape>
            </item>
            <item>
                <shape android:shape="rectangle">
                    <corners android:radius="14dp" />
                    <gradient
                        android:type="linear"
                        android:angle="90"
                        android:startColor="@color/cinematic_cyan"
                        android:endColor="@color/cinematic_cyan_light" />
                    <stroke android:width="2dp" android:color="#90FFFFFF" />
                </shape>
            </item>
        </layer-list>
    </item>

    <!-- PRESSED state -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <corners android:radius="14dp" />
            <solid android:color="@color/cinematic_cyan_dark" />
        </shape>
    </item>

    <!-- DEFAULT state -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="14dp" />
            <gradient
                android:type="linear"
                android:angle="90"
                android:startColor="@color/cinematic_cyan_dark"
                android:endColor="@color/cinematic_cyan" />
        </shape>
    </item>

</selector>
