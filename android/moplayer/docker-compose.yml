version: '3.8'

services:
  backend:
    build: ./backend
    container_name: moplayer-backend
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
    restart: unless-stopped
