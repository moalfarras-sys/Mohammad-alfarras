<?xml version="1.0" encoding="utf-8"?>
<!-- Focus glow ring for TV navigation — Neon Cyan halo -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <stroke
        android:width="2.5dp"
        android:color="#00F0FF" />
    <!-- Glow via shadow color (API 28+) or use as layer-list in caller -->
    <padding
        android:left="6dp"
        android:right="6dp"
        android:top="6dp"
        android:bottom="6dp" />
    <corners android:radius="24dp" />
</shape>
